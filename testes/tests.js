class Calculadora {
    constructor(...numeros) {
        const display = document.querySelector('.display');
    }

    display.addEventListener('click', e => {
        const elemento = e.target;
    });
}
